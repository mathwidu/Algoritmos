#include <stdio.h>
#include <locale.h>
#include <math.h>
////////////////////////////////////////////////////////
int main (){
    setlocale(LC_ALL, "");
////////////////////////////////////////////////////////
    printf("digite sua altura: ");
    double altura;
    scanf("%lf", &altura);
////////////////////////////////////////////////////////
    printf("digite seu peso: ");
    double peso;
    scanf("%lf", &peso);
////////////////////////////////////////////////////////
    double imc = peso/pow(altura, 2);
////////////////////////////////////////////////////////
    printf("seu imc é %lf\n", imc);
////////////////////////////////////////////////////////
    if (imc < 18.5){
        printf("Você está abaixo do peso normal");
    }
////////////////////////////////////////////////////////
    if (imc >= 18.5 && imc <= 24.9){
        printf("Você está com o peso normal\n");
    }
////////////////////////////////////////////////////////
    if (imc >= 25.0 && imc <=29.9){
        printf("Você está e, excesso de peso\n");
    }
////////////////////////////////////////////////////////
    if (imc >= 30.0 && imc <=34.9){
        printf("Você está em obesidade classe 1\n");
     }
////////////////////////////////////////////////////////
    if (imc >= 35.0 && imc <=39.9){
        printf("Você está em obesidade classe 2\n");
     }
////////////////////////////////////////////////////////
    if (imc > 40.0) {
        printf("Você está em obesidade classe 3\n");
     }
////////////////////////////////////////////////////////
    return 0;
////////////////////////////////////////////////////////
}
















